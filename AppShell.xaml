<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GregModmanager.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:l="clr-namespace:GregModmanager.Localization"
    Title="gregModmanager"
    FlyoutBehavior="Locked"
    FlyoutWidth="220"
    FlyoutBackgroundColor="{DynamicResource SurfaceContainerLow}">

    <Shell.FlyoutHeader>
        <VerticalStackLayout Padding="20,24,20,16" Spacing="8">
            <Label
                Text="gregModmanager"
                FontFamily="SpaceGrotesk-Bold"
                FontSize="18"
                TextColor="{DynamicResource Primary}"
                CharacterSpacing="0.4" />
            <Label
                Text="Workshop Manager"
                FontFamily="SpaceGrotesk-Regular"
                FontSize="11"
                TextColor="{DynamicResource OnSurfaceVariant}" />
        </VerticalStackLayout>
    </Shell.FlyoutHeader>

    <Shell.FlyoutFooter>
        <VerticalStackLayout Padding="16,8,16,16">
            <Border
                x:Name="SteamLogoTile"
                Padding="10,6"
                BackgroundColor="{DynamicResource SurfaceVariant}"
                StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="6" />
                </Border.StrokeShape>
                <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                    <Ellipse
                        x:Name="SteamStatusLed"
                        WidthRequest="8"
                        HeightRequest="8"
                        VerticalOptions="Center" />
                    <Label
                        x:Name="SteamStatusText"
                        FontFamily="SpaceGrotesk-Regular"
                        FontSize="11"
                        LineBreakMode="NoWrap"
                        Text="{l:Translate Key=Steam_Connecting}"
                        TextColor="{DynamicResource OnSurfaceDim}"
                        VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>

            <!-- GREGAPI CONNECTION TILE -->
            <Border
                x:Name="GregApiLogoTile"
                Padding="10,6"
                Margin="0,8,0,0"
                BackgroundColor="{DynamicResource SurfaceVariant}"
                StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="6" />
                </Border.StrokeShape>
                <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                    <Ellipse
                        x:Name="GregApiStatusLed"
                        WidthRequest="8"
                        HeightRequest="8"
                        VerticalOptions="Center" />
                    <Label
                        x:Name="GregApiStatusText"
                        FontFamily="SpaceGrotesk-Regular"
                        FontSize="11"
                        LineBreakMode="NoWrap"
                        Text="gregAPI Offline"
                        TextColor="{DynamicResource OnSurfaceDim}"
                        VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>

            <!-- AUTH ACCOUNT TILE -->
            <Border
                x:Name="AuthLogoTile"
                Padding="10,6"
                Margin="0,8,0,0"
                BackgroundColor="{DynamicResource SurfaceVariant}"
                StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="6" />
                </Border.StrokeShape>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnAuthTileTapped" />
                </Border.GestureRecognizers>
                <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
                    <Ellipse
                        x:Name="AuthStatusLed"
                        WidthRequest="8"
                        HeightRequest="8"
                        VerticalOptions="Center"
                        Fill="{DynamicResource OnSurfaceDim}" />
                    <Label
                        x:Name="AuthStatusText"
                        FontFamily="SpaceGrotesk-Regular"
                        FontSize="11"
                        LineBreakMode="NoWrap"
                        Text="Login To Datacentermods.com"
                        TextColor="{DynamicResource OnSurfaceDim}"
                        VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>
        </VerticalStackLayout>
    </Shell.FlyoutFooter>

    <Shell.ItemTemplate>
        <DataTemplate x:DataType="BaseShellItem">
            <Grid
                ColumnDefinitions="4,*"
                HeightRequest="44"
                Padding="0,2">
                <BoxView
                    Grid.Column="0"
                    WidthRequest="4"
                    Color="{DynamicResource Primary}"
                    CornerRadius="2"
                    VerticalOptions="Center"
                    HeightRequest="24"
                    IsVisible="{Binding IsChecked}" />
                <Label
                    Grid.Column="1"
                    Text="{Binding Title}"
                    FontFamily="SpaceGrotesk-Medium"
                    FontSize="13"
                    TextColor="{DynamicResource OnSurface}"
                    VerticalTextAlignment="Center"
                    Padding="12,0,0,0" />
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

</Shell>






