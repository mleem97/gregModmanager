Place the compiled MelonLoader helper mod here:

  SubDirectoryFixer.dll

At runtime, gregModmanager installs this DLL on first app start to:

  <GameRoot>\Mods\SubDirectoryFixer.dll

If the DLL is missing, installation is skipped and logged.